# spritz-cli

CLI for interacting with the Spritz API.

## Install

```bash
curl --proto '=https' --tlsv1.2 --fail --silent --show-error --location https://static.spritz.finance/install.sh | bash
```

The installer verifies Sigstore-signed release checksums before installing the binary.
